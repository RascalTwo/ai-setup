# r2-sdlc reviewer subagents

This .ruler workspace exists only to compile the reviewer subagents in `agents/`
into Claude (`.claude/agents/*.md`) and Codex (`.codex/agents/*.toml`) native formats.
Not a rules source.
